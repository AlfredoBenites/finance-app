# Copy to .env.local.
# Base URL of the FastAPI backend.
VITE_API_BASE_URL=http://localhost:8000

# Supabase Auth (login/signup only). The anon key is a public key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
